name: 🐍 Generate Snake Animation

on:
  schedule:
    - cron: "0 0 * * *"  # مرة يومياً
  workflow_dispatch:  # يمكنك تشغيله يدوياً

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: ⬇️ Checkout Repository
        uses: actions/checkout@v4
        
      - name: 🎨 Generate Snake Graphics
        uses: Platane/snk@v3
        with:
          github_user_name: ahmedsabrari
          outputs: |
            snake/snake.svg
            snake/snake.gif
          svg_color: "#0D1117:#161B22:#21262D:#30363D:#8B949E:#C9D1D9:#ECF2F9:#FFFFFF"
          gif_color: "dark"
          
      - name: 📤 Commit & Push Changes
        run: |
          git config --local user.name "github-actions[bot]"
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          
          # إضافة ملفات الثعبان فقط
          git add snake/
          
          # التحقق من وجود تغييرات
          if ! git diff --staged --quiet; then
            git commit -m "🐍 تحديث رسم الثعبان $(date '+%Y-%m-%d %H:%M')"
            git push
          else
            echo "✅ لا توجد تغييرات جديدة"
          fi
